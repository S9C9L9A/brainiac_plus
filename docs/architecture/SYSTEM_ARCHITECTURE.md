# 🏗️ Architettura del Sistema BraniacPlus

## 📊 Vista d'Insieme

```
┌─────────────────────────────────────────────────────────────────┐
│                       BRAINIAC PLUS                             │
│                    (Mobile App Flutter)                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │               lib/core/network/                            │ │
│  │            api_client.dart (HTTP Client)                  │ │
│  │                                                             │ │
│  │  • FacebookAuthService                                    │ │
│  │  • Richieste HTTPS al backend                             │ │
│  │  • Gestione errori                                        │ │
│  └────────────────────────────────────────────────────────────┘ │
│                           │                                      │
└─────────────────────────────────────────────────────────────────┘
                            │
         POST /api/facebook/auth
         POST /api/facebook/post
         GET  /api/facebook/pages
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                  GO BACKEND (go_backend/)                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────────────┐  ┌────────────────────┐                │
│  │   ROUTES LAYER     │  │  MIDDLEWARE LAYER  │                │
│  ├────────────────────┤  ├────────────────────┤                │
│  │ routes/            │  │ • CORS              │                │
│  │ └─ facebook.go     │  │ • JWT Validation    │                │
│  │    ├─ /auth        │  │ • Error Handling    │                │
│  │    ├─ /pages       │  │                     │                │
│  │    └─ /post        │  │ config.go           │                │
│  │                    │  │ • .env loading      │                │
│  │ routes/            │  │ • Env validation    │                │
│  │ └─ middleware.go   │  │                     │                │
│  └────────────────────┘  └────────────────────┘                │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │             SERVICES LAYER (Logica di Business)            │ │
│  ├────────────────────────────────────────────────────────────┤ │
│  │                                                             │ │
│  │  services/facebook.go                                      │ │
│  │  ├─ NewFacebookService()                                   │ │
│  │  ├─ ValidateUserToken()  ──────┐                           │ │
│  │  ├─ GetUserInfo()         ──────┼──→ Facebook Graph API    │ │
│  │  ├─ GetUserPages()        ──────┤   https://graph.         │ │
│  │  └─ PostToPage()          ──────┘   facebook.com/          │ │
│  │                                                             │ │
│  │  services/jwt.go                                           │ │
│  │  ├─ GenerateToken()                                        │ │
│  │  └─ ValidateToken()                                        │ │
│  │                                                             │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │            MODELS LAYER (Strutture Dati)                  │ │
│  ├────────────────────────────────────────────────────────────┤ │
│  │                                                             │ │
│  │  models/facebook.go                                        │ │
│  │  ├─ FacebookUser                                           │ │
│  │  ├─ FacebookAuthRequest                                    │ │
│  │  ├─ FacebookAuthResponse                                   │ │
│  │  ├─ FacebookTokenValidation                                │ │
│  │  └─ FacebookUserData                                       │ │
│  │                                                             │ │
│  │  services/jwt.go                                           │ │
│  │  └─ JWTClaims                                              │ │
│  │                                                             │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │          CONFIGURATION (Variabili d'Ambiente)              │ │
│  ├────────────────────────────────────────────────────────────┤ │
│  │                                                             │ │
│  │  .env (NON COMMITTARE!)                                    │ │
│  │  ├─ FACEBOOK_TOKEN        ← Token di accesso FB            │ │
│  │  ├─ FACEBOOK_APP_ID       ← ID dell'app                    │ │
│  │  ├─ FACEBOOK_APP_SECRET   ← Secret dell'app                │ │
│  │  ├─ JWT_SECRET            ← Secret per JWT                 │ │
│  │  └─ PORT                  ← Porta (default 8080)           │ │
│  │                                                             │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
                            │
         Usa token da .env
         per comunicare con
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│              FACEBOOK GRAPH API                                  │
│              https://graph.facebook.com/v18.0/                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  • Valida i token (debug_token)                                │
│  • Recupera info utente                                         │
│  • Gestisce pagine e post                                      │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Flusso di Dati: Autenticazione

```
┌──────────┐
│  USER    │
└────┬─────┘
     │
     │ 1. Clicca "Login con Facebook"
     │
     ▼
┌────────────────────────┐
│ Flutter Facebook SDK   │
│ (Facebook App Dialog)  │
└────────┬───────────────┘
         │
         │ 2. Ritorna: accessToken
         │
         ▼
┌─────────────────────────────────────────┐
│ api_client.dart:                        │
│ FacebookAuthService.authenticateWithFB  │
│ (POST /api/facebook/auth)               │
└────────────┬────────────────────────────┘
             │
             │ 3. Richiesta HTTP
             │ {access_token, user_id}
             │
             ▼
┌─────────────────────────────────────────┐
��� routes/facebook.go:                     │
│ handleFacebookAuth()                    │
│ - Legge il JSON                         │
│ - Chiama il service                     │
└────────────┬────────────────────────────┘
             │
             │ 4. Delega al service
             │
             ▼
┌─────────────────────────────────────────┐
│ services/facebook.go:                   │
│ ValidateUserToken()                     │
│ - Prepara URL di validazione            │
│ - Usa FACEBOOK_TOKEN dal .env           │
└────────────┬────────────────────────────┘
             │
             │ 5. Richiesta HTTPS
             │ GET /debug_token
             │     ?input_token=USER_TOKEN
             │     &access_token=APP_TOKEN
             │
             ▼
        FACEBOOK
        API
             │
             │ 6. Risposta: {is_valid: true}
             │
             ▼
┌─────────────────────────────────────────┐
│ services/facebook.go:                   │
│ GetUserInfo()                           │
│ - Recupera info utente                  │
└────────────┬────────────────────────────┘
             │
             │ 7. Richiesta HTTPS
             │ GET /{user_id}
             │     ?fields=id,name,email
             │
             ▼
        FACEBOOK
        API
             │
             │ 8. Risposta: {id, name, email}
             │
             ▼
┌─────────────────────────────────────────┐
│ services/jwt.go:                        │
│ GenerateToken()                         │
│ - Crea JWT con user data                │
│ - Scadenza: 24 ore                      │
└────────────┬────────────────────────────┘
             │
             │ 9. JWT generato
             │
             ▼
┌─────────────────────────────────────────┐
│ routes/facebook.go:                     │
│ Ritorna FacebookAuthResponse            │
│ {valid: true, user: {...}, token: JWT}  │
└────────────┬────────────────────────────┘
             │
             │ 10. Response HTTP JSON
             │
             ▼
┌─────────────────────────────────────────┐
│ api_client.dart:                        │
│ Riceve la risposta                      │
└────────────┬────────────────────────────┘
             │
             │ 11. Salva JWT
             │
             ▼
┌─────────────────────────────────────────┐
│ flutter_secure_storage                  │
│ (Salva il token in modo sicuro)         │
└────────────┬────────────────────────────┘
             │
             │ 12. Utente autenticato ✅
             │
             ▼
        HOME SCREEN
```

---

## 📝 Struttura File Completa

```
brainiac_plus/
├── 📄 GO_BACKEND_GUIDE.md              ← Leggi questa!
├── 📄 FLUTTER_BACKEND_INTEGRATION.md   ← E questa!
├── 📄 BACKEND_SETUP_SUMMARY.md         ← Riepilogo
├── 📄 INSTALLATION_GUIDE.md            ← Come installare
│
├── go_backend/
│   ├── main.go                          ← Entry point
│   ├── config.go                        ← Carica .env
│   ├── go.mod                           ← Dipendenze
│   ├── .env.example                     ← Template
│   ├── .env                             ← Config reale (NON COMMITTARE)
│   │
│   ├── models/
│   │   ├── facebook.go                  ← Strutture Facebook
│   │   └── ollama.go                    ← (Futuro)
│   │
│   ├── services/
│   │   ├── facebook.go                  ← Logica Facebook
│   │   ├── jwt.go                       ← Gestione JWT
│   │   └── ollama.go                    ← (Futuro)
│   │
│   └── routes/
│       ├── facebook.go                  ← Endpoint Facebook
│       ├── middleware.go                ← Middleware auth
│       └── ollama.go                    ← (Futuro)
│
└── lib/
    └── core/
        └── network/
            └── api_client.dart          ← Client HTTP + Servizi
```

---

## 🔐 Flusso di Sicurezza

```
┌─────────────────┐
│ User Input      │
└────────┬────────┘
         │
         ▼
    ╔═════════════════════════════╗
    ║ VALIDATION IN FLUTTER       ║
    ║ - Controlla input           ║
    ║ - Sanitizza dati            ║
    ╚────────────┬────────────────╝
                 │
                 ▼
         ╔═══════════════════════╗
         ║ HTTPS SOLO ✅         ║
         ║ (Produzione)          ║
         ╚───────────┬───────────╝
                     │
                     ▼
         ╔═══════════════════════════════════════════╗
         ║ BACKEND GO                                ║
         ║ - Token da VARIABILE d'AMBIENTE (non env) ║
         ║ - NON nel codice                          ║
         ║ - MAI in logs                             ║
         ╚───────────┬───────────────────────────────╝
                     │
                     ▼
         ╔═══════════════════════╗
         ║ FACEBOOK GRAPH API    ║
         ║ - Valida il token     ║
         ║ - Rate limiting       ║
         ╚───────────┬───────────╝
                     │
                     ▼
         ╔═══════════════════════╗
         ║ JWT GENERATO          ║
         ║ - Firmato             ║
         ║ - Con scadenza        ║
         ║ - Secret nel backend  ║
         ╚───────────┬───────────╝
                     │
                     ▼
         ╔═══════════════════════╗
         ║ FLUTTER SECURE        ║
         ║ STORAGE               ║
         ║ - Encrypted           ║
         ║ - Platform-specific   ║
         ╚───────────────────────╝
```

---

## 🔄 Richieste Autenticate

```
1️⃣ User ha JWT salvato in SecureStorage

2️⃣ Vuole fare un'azione autenticata
   (es: pubblicare un post)

3️⃣ Flutter legge JWT

4️⃣ Aggiunge al header:
   Authorization: Bearer eyJhbGc...

5️⃣ Invia richiesta:
   POST /api/facebook/post
   Headers: {
     Content-Type: application/json
     Authorization: Bearer eyJhbGc...
   }
   Body: {
     page_id: "...",
     page_token: "...",
     message: "..."
   }

6️⃣ Backend middleware (routes/middleware.go):
   - Estrae il token dal header
   - Valida con JWTService
   - Se valido, continua
   - Se non valido, ritorna 401

7️⃣ Handler processa la richiesta

8️⃣ Risposta ritorna al Flutter
```

---

## 📚 Layer Pattern (Architettura Pulita)

```
┌─────────────────────────────────┐
│ 4. ROUTES LAYER (routes/*)      │  ← Input/Output
│    Riceve HTTP request          │
│    Valida parametri             │
│    Ritorna response             │
└────────────────┬────────────────┘
                 │
┌────────────────▼────────────────┐
│ 3. SERVICES LAYER (services/*)  │  ← Logica di Business
│    Implementa la logica         │
│    Comunica con API esterne     │
│    Ritorna dati al route        │
└────────────────┬────────────────┘
                 │
┌────────────────▼────────────────┐
│ 2. MODELS LAYER (models/*)      │  ← Strutture Dati
│    Definisce i tipi             │
│    Serializzazione JSON         │
│    Type-safe                    │
└────────────────┬────────────────┘
                 │
┌────────────────▼────────────────┐
│ 1. CONFIG LAYER (config.go)     │  ← Configurazione
│    Carica variabili ambiente    │
│    Validazione                  │
└─────────────────────────────────┘
```

**Vantaggi di questo pattern:**
- ✅ Separazione dei compiti (SoC)
- ✅ Facile da testare
- ✅ Facile da mantenere
- ✅ Facile da scalare
- ✅ Riusabile

---

## 🚀 Deploy Architecture (Futuro)

```
┌─────────────────────────────────────────┐
│ Flutter App                             │
│ (iOS/Android/Web)                       │
└──────────────┬──────────────────────────┘
               │ HTTPS
               │ (Certificati SSL/TLS)
               ▼
┌──────────────────────────────┐
│ Cloud Load Balancer          │
│ (Railway/Heroku/AWS)         │
└──────────────┬───────────────┘
               │
               ▼
┌──────────────────────────────┐
│ Go Backend Instances         │
│ (Auto-scaling)               │
│ • 3-5 repliche per HA        │
│ • Variabili in env manager   │
└──────────────┬───────────────┘
               │
               ▼
┌──────────────────────────────┐
│ Redis Cache (Opzionale)      │
│ • Rate limiting              │
│ • Session cache              │
└──────────────┬───────────────┘
               │
               ▼
┌──────────────────────────────┐
│ PostgreSQL Database          │
│ • User data                  │
│ • Sessions                   │
│ • Logs                       │
└──────────────┬───────────────┘
               │
               ▼
┌──────────────────────────────┐
│ Facebook Graph API           │
│ (Esterno)                    │
└──────────────────────────────┘
```

---

## ✨ Tecnologie Utilizzate

| Componente | Tecnologia | Versione |
|-----------|-----------|---------|
| Backend | Go | 1.21+ |
| Framework Web | Gin | 1.10.0 |
| Autenticazione | JWT | v5 |
| Caricamento Env | godotenv | 1.5.1 |
| Frontend | Flutter | 3.x+ |
| HTTP Client | http | 1.1.0+ |
| Facebook SDK | flutter_facebook_sdk | 6.0.0+ |
| Secure Storage | flutter_secure_storage | 9.0.0+ |

---

## 🎯 Prossimi Passi

1. **MVP** → Autenticazione Facebook + Pubblicazione Post
2. **Fase 2** → Integrazione Ollama (AI)
3. **Fase 3** → Integrazione Instagram/TikTok
4. **Fase 4** → Dashboard Analytics
5. **Fase 5** → Deploy Produzione

---

Perfetto! Ora hai l'intero quadro! 🎉
